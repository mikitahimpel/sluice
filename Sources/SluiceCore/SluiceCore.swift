/// Sluice's pure-Swift core: rule model, rule engine, router, persistence.
/// No AppKit / Launch Services dependencies — testable via `swift test`.
public enum SluiceCore {
    public static let version = "0.1.0"
}
